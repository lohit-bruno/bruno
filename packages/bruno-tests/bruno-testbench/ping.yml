info:
  name: ping
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/ping"

runtime:
  scripts:
    - type: before-request
      code: bru.runner.stopExecution();

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
