info:
  name: request
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:6000/test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
