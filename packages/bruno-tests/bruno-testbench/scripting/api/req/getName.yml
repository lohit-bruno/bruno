info:
  name: getName
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/ping"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Check if request name is getName", function () {
            expect(req.getName()).to.eql("getName");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
